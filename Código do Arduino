====================================================================================
void setup() {
  pinMode(12, OUTPUT);
}

void loop() {
  digitalWrite(12, HIGH);
  delay(1000);
  digitalWrite(12, LOW);
  delay(1000);
}
==========================================================================

CONEXÃO DO ARDUINO:

#include <ESP8266WiFi.h>

const char* ssid = "Cyber-Projeto";
const char* password = "Senai@122";

void setup() {

  Serial.begin(115200);

  Serial.println();
  Serial.println("Conectando ao WiFi...");

  WiFi.begin(ssid, password);

  while (WiFi.status() != WL_CONNECTED) {

    delay(1000);
    Serial.print(".");

  }

  Serial.println();
  Serial.println("WiFi conectado!");

  Serial.print("IP: ");
  Serial.println(WiFi.localIP());

}

void loop() {

}
=====================================================================================

CÓDIGO DO PYTHON PARA INTEGRAR O ARDUINO:

import mysql.connector

conexao = mysql.connector.connect(
    host="localhost",
    user="root",
    password="root",
    database="Alpha_Tech"
)

cursor = conexao.cursor()

sql = """
INSERT INTO sensores (nome, localizacao, status)
VALUES (%s, %s, %s)
"""

valores = ("DHT212", "Sala 1", "Ativo")

cursor.execute(sql, valores)

conexao.commit()

print("Sensor cadastro")
